import FlutterMacOS
import Foundation

func RegisterGeneratedPlugins(registry: FlutterPluginRegistry) {
  // No-op when there are no plugins.
  // Overridden per-application by the plugin registrant generator.
}
